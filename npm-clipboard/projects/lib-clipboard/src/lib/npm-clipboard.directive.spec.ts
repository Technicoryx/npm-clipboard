import { NpmClipboardDirective } from './npm-clipboard.directive';

describe('NpmClipboardDirective', () => {
  it('should create an instance', () => {
    const directive = new NpmClipboardDirective();
    expect(directive).toBeTruthy();
  });
});
